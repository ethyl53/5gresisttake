const {SlashCommandBuilder,PermissionFlagsBits}=require('discord.js');const db=require('../database/db');const {stopActivity}=require('../database/intervalService');
module.exports={data:new SlashCommandBuilder().setName('admin-stop').setDescription('指定ユーザーの作業を停止').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addUserOption(o=>o.setName('user').setDescription('ユーザー').setRequired(true)),async execute(i){await i.deferReply();try{const u=i.options.getUser('user'),r=await stopActivity(db,{guildId:i.guildId||'',userId:u.id});await i.editReply(r.kind==='none'?`${u.username} は作業中ではありません。`:`${u.username} の作業を停止しました。`);i.client.persistentRanking?.update?.();}catch(e){console.error(e);await i.editReply('停止に失敗しました。');}}};
